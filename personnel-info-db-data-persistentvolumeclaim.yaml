apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: personnel-info-db-data
  name: personnel-info-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
