apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: leave-request-db-data
  name: leave-request-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
